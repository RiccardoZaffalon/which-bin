Scrape a website to know which trash bin you should leave out for the next day.

## Setup
Use the following environment variables to configure the app:

```bash
WHICHBIN_SCRAPE_URL=http://somesite.com/calendar
```